<!DOCTYPE html>
<html>
    <head>
        <script src="/socket.io/socket.io.js"></script>
        <script src="https://code.jquery.com/jquery-1.11.1.js"></script>
    </head>
    <body>
            <img src="" id="image" name="image" class="image"/>
</ul>

    </body>


    <script>
       
            $(function() {
                var imagepath = "";
                var socket = io();
                var stack = [
        {
          id: 1,
          path: "https://picsum.photos/id/40/400/400"
        },
        {
          id: 2,
          path: "https://picsum.photos/id/77/400/400"
        },
        {
          id: 3,
          path: "https://picsum.photos/id/200/400/400"
        },
        {
          id: 4,
          path: "https://picsum.photos/id/230/400/400"
        },
        {
          id: 5,
          path: "https://picsum.photos/id/250/400/400"
        }
      ]      
      document.getElementById('image').src = stack[2].path;
             socket.on('user join', function (msg){
                });
                socket.on('reMessage', function (data){
                    if(data.category === 'Natur'){
                    document.body.style.background = 'green';
                    } 
                    if(data.category === 'Familie'){
                    document.body.style.background = 'red';
                    } 
                    if(data.category === 'Regional'){
                    document.body.style.background = 'orange';
                    } 
                    if(data.category === 'Freizeit'){
                    document.body.style.background = 'blue';
                    } 
                    imagepath = data.bild1path;
                });
                socket.on('retvStack', function (data){
                    document.getElementById('image').src = stack[data].path;
                });
            });   
                </script>

<style type="text/css">
    .image{
    position: fixed;
    top: 0;
    bottom: 0;
    left: 0;
    right: 0;
    height:100%;
    width: auto;
    margin: auto;
    overflow: auto;
    }
    </style>
</html>